# dotfiles

# my memo
- cd $HOME
* git clone <URL>
+ ln -s dotfiles/.vimrc ./
- ln -s dotfiles/.tmux.conf ./
*   .
+   .
-   .
* cd dotfiles
+ git config user.name <USERNAME>
- git config user.email <MAIL@ADDRESS>
